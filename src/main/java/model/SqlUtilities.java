package model;
import java.util.UUID;

import javax.management.Query;
import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;
import javax.persistence.TypedQuery;

import model.Postit;
import model.User;
import eclipselink.service.*;

import java.util.*;

/**
 * Utility class for Database Operations like populating a Database with random generated Users.
 * @author Chris
 *
 */

/*
public class SqlUtilities {
	
	public static void populateDatabase(int amount)
	{
		

		
		
		UserManagement.CreateUser(email, password, avatar, name, surname, position, phone, birthDate, gender, registerDate, fax, location, postits);
	}

}
*/
